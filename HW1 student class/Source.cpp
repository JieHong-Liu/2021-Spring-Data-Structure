#include <iostream>
#include <fstream>
#include "student.h"
using namespace std;
int main() 
{
	//student s[10]; /* Static memory allocation, but we use dynamic mem allocation */
	//process1();  /* consol mode execution*/
	file_process(); /* file open exeution*/
	find();
	system("pause");
}